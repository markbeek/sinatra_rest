Ccreated simple sinatra/json/rest service,
using a YAML "database" for simplicity's sake.

That was a proof-of-concept, which has now been relegated to the yaml branch.

The master branch will be refactored to use mongoDB instead of YAML files.

